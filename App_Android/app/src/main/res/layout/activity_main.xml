<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.local.app_android.MainActivity">

    <TextView
        android:id="@+id/sample_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Adresse IP Publique :"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="107dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="X.X.X.X"
        android:id="@+id/ip_text"
        android:layout_marginTop="25dp"
        android:layout_alignTop="@+id/sample_text"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="90dp"
        android:layout_height="50dp"
        android:layout_marginTop="12dp"
        android:id="@+id/bouton"
        android:layout_below="@+id/ip_text"
        android:layout_centerHorizontal="true"
        android:text="Refresh" />

    <VideoView
        android:layout_width="150dp"
        android:layout_height="80dp"
        android:layout_marginTop="26dp"
        android:id="@+id/videoView"
        android:layout_below="@+id/bouton"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
